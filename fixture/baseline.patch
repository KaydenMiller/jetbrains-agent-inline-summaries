diff --git a/Assets/Scripts/CameraFollow.cs b/Assets/Scripts/CameraFollow.cs
index e57e6ec..1d79f16 100644
--- a/Assets/Scripts/CameraFollow.cs
+++ b/Assets/Scripts/CameraFollow.cs
@@ -1,3 +1,4 @@
+using Fixture.Player;
 using UnityEngine;
 
 namespace Fixture.Cameras
@@ -20,7 +21,11 @@ namespace Fixture.Cameras
         [SerializeField]
         private float lookDamping = 18f;
 
+        [SerializeField]
+        private float landingDipDamping = 4.5f;
+
         private Vector3 followVelocity;
+        private PlayerController player;
 
         private void LateUpdate()
         {
@@ -34,17 +39,22 @@ namespace Fixture.Cameras
         }
 
         /// <summary>
-        /// Moves the camera towards the target's offset position.
+        /// Moves the camera towards the target's offset position, damping the vertical
+        /// axis separately while the target is descending.
         /// </summary>
         private void FollowTarget()
         {
             Vector3 desired = target.TransformPoint(offset);
+            float verticalRate = player != null && !player.IsGrounded ? landingDipDamping : followDamping;
 
-            transform.position = Vector3.SmoothDamp(
+            Vector3 next = Vector3.SmoothDamp(
                 transform.position,
                 desired,
                 ref followVelocity,
                 1f / followDamping);
+
+            next.y = Mathf.Lerp(transform.position.y, desired.y, verticalRate * Time.deltaTime);
+            transform.position = next;
         }
 
         /// <summary>
@@ -60,20 +70,5 @@ namespace Fixture.Cameras
                 desired,
                 lookDamping * Time.deltaTime);
         }
-
-        /// <summary>
-        /// Draws the follow target and the desired camera position while selected.
-        /// </summary>
-        private void OnDrawGizmosSelected()
-        {
-            if (target == null)
-            {
-                return;
-            }
-
-            Gizmos.color = Color.cyan;
-            Gizmos.DrawWireSphere(target.TransformPoint(offset), 0.15f);
-            Gizmos.DrawLine(transform.position, target.position);
-        }
     }
 }
diff --git a/Assets/Scripts/Generated/TerrainTileTable.cs b/Assets/Scripts/Generated/TerrainTileTable.cs
index d538b10..93268d6 100644
--- a/Assets/Scripts/Generated/TerrainTileTable.cs
+++ b/Assets/Scripts/Generated/TerrainTileTable.cs
@@ -1226,7 +1226,7 @@ namespace Fixture.Terrain.Generated
         public static int TileAt(Vector3 world)
         {
             int column = Mathf.Clamp(Mathf.FloorToInt(world.x / 64f), 0, 24);
-            int row = Mathf.Clamp(Mathf.FloorToInt(world.z / 64f), 0, 23);
+            int row = Mathf.Clamp(Mathf.FloorToInt(world.z / 64f), 0, 24);
             return (row * 25) + column;
         }
     }
diff --git a/Assets/Scripts/Legacy/LegacyCameraShake.cs b/Assets/Scripts/Legacy/LegacyCameraShake.cs
deleted file mode 100644
index f0b2920..0000000
--- a/Assets/Scripts/Legacy/LegacyCameraShake.cs
+++ /dev/null
@@ -1,32 +0,0 @@
-using UnityEngine;
-
-namespace Fixture.Cameras.Legacy
-{
-    /// <summary>
-    /// Pre-Cinemachine screen shake, driven by an explicit AddShake call from gameplay
-    /// code. Superseded by CameraFollow's landing dip.
-    /// </summary>
-    public class LegacyCameraShake : MonoBehaviour
-    {
-        [SerializeField]
-        private float decay = 4f;
-
-        private float amplitude;
-
-        public void AddShake(float amount)
-        {
-            amplitude = Mathf.Max(amplitude, amount);
-        }
-
-        private void LateUpdate()
-        {
-            if (amplitude <= 0.0001f)
-            {
-                return;
-            }
-
-            transform.localPosition = Random.insideUnitSphere * amplitude;
-            amplitude = Mathf.MoveTowards(amplitude, 0f, decay * Time.deltaTime);
-        }
-    }
-}
diff --git a/Assets/Scripts/Legacy/LegacyCameraShake.cs.meta b/Assets/Scripts/Legacy/LegacyCameraShake.cs.meta
deleted file mode 100644
index 2621bef..0000000
--- a/Assets/Scripts/Legacy/LegacyCameraShake.cs.meta
+++ /dev/null
@@ -1,11 +0,0 @@
-fileFormatVersion: 2
-guid: 71be68d9f76e9ba27a5395d6b97d85ca
-MonoImporter:
-  externalObjects: {}
-  serializedVersion: 2
-  defaultReferences: []
-  executionOrder: 0
-  icon: {instanceID: 0}
-  userData: 
-  assetBundleName: 
-  assetBundleVariant: 
diff --git a/Assets/Scripts/PlayerController.cs b/Assets/Scripts/PlayerController.cs
index eada6a9..0d916ee 100644
--- a/Assets/Scripts/PlayerController.cs
+++ b/Assets/Scripts/PlayerController.cs
@@ -27,6 +27,11 @@ namespace Fixture.Player
         [SerializeField]
         private float jumpImpulse = 6.4f;
 
+        [SerializeField]
+        private float jumpBufferMs = 120f;
+        [SerializeField]
+        private float coyoteTimeMs = 90f;
+
         [SerializeField]
         private LayerMask groundLayers = ~0;
 
@@ -43,6 +48,8 @@ namespace Fixture.Player
         private Vector2 moveInput;
         private bool jumpPressedThisFrame;
         private bool grounded;
+        private float jumpBufferedUntil;
+        private float lastGroundedAt;
 
         /// <summary>True while the capsule is standing on something in groundLayers.</summary>
         public bool IsGrounded => grounded;
@@ -61,7 +68,7 @@ namespace Fixture.Player
         {
             moveInput = new Vector2(Input.GetAxisRaw("Horizontal"), Input.GetAxisRaw("Vertical"));
 
-            if (Input.GetButtonDown("Jump"))
+            if (Input.GetButtonDown("Jump") || Input.GetButtonDown("Submit"))
             {
                 jumpPressedThisFrame = true;
             }
@@ -70,6 +77,12 @@ namespace Fixture.Player
         private void FixedUpdate()
         {
             grounded = GroundCheck();
+
+            if (grounded)
+            {
+                lastGroundedAt = Time.time;
+            }
+
             HandleMove();
             HandleJump();
         }
@@ -94,27 +107,6 @@ namespace Fixture.Player
             body.velocity = new Vector3(delta.x, body.velocity.y, delta.z);
         }
 
-        /// <summary>
-        /// Applies the jump impulse when a press is pending and the capsule is grounded.
-        /// </summary>
-        private void HandleJump()
-        {
-            if (!jumpPressedThisFrame)
-            {
-                return;
-            }
-
-            jumpPressedThisFrame = false;
-
-            if (!grounded)
-            {
-                return;
-            }
-
-            body.velocity = new Vector3(body.velocity.x, 0f, body.velocity.z);
-            body.AddForce(Vector3.up * jumpImpulse, ForceMode.VelocityChange);
-        }
-
         /// <summary>
         /// Sphere-casts down from the base of the capsule and reports whether anything in
         /// groundLayers is within groundProbeDistance.
@@ -132,5 +124,34 @@ namespace Fixture.Player
                 groundLayers,
                 QueryTriggerInteraction.Ignore);
         }
+
+        /// <summary>
+        /// Applies the jump impulse when a press is pending, holding that press for
+        /// jumpBufferMs so it survives the frames where the capsule is still landing.
+        /// </summary>
+        private void HandleJump()
+        {
+            if (jumpPressedThisFrame)
+            {
+                jumpBufferedUntil = Time.time + (jumpBufferMs / 1000f);
+                jumpPressedThisFrame = false;
+            }
+
+            if (Time.time > jumpBufferedUntil)
+            {
+                return;
+            }
+
+            bool withinCoyoteTime = Time.time <= lastGroundedAt + (coyoteTimeMs / 1000f);
+
+            if (!grounded && !withinCoyoteTime)
+            {
+                return;
+            }
+
+            jumpBufferedUntil = 0f;
+            body.velocity = new Vector3(body.velocity.x, 0f, body.velocity.z);
+            body.AddForce(Vector3.up * jumpImpulse, ForceMode.VelocityChange);
+        }
     }
 }
