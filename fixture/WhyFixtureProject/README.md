# WhyFixtureProject

A deliberately small Unity-shaped C# project used to exercise the `.why` plugin by hand.
It is not a runnable Unity project: there are no scenes, no packages and no Library/.
Its only job is to hold source files that `.why` notes point at.

The checks to run against it, in order, live in `../RUNBOOK.md`. Read that first.
