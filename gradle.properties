org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=768m
org.gradle.caching=true
org.gradle.configuration-cache=false

kotlin.stdlib.default.dependency=false

pluginVersion=0.1.0
platformType=IC
platformVersion=2026.1.5
# Local convenience: verifyPlugin and runRider use this Rider install when it exists.
# A machine without it (a CI runner, or another developer) falls back to the
# build platform above — see the pluginVerification block in build.gradle.kts.
riderLocalPath=/Users/kaydenmiller/Applications/Rider.app
